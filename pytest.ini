[pytest]
# Default `pytest` only runs offline CI smoke tests. Other files under tests/ are scripts or integration checks.
testpaths = tests
python_files = test_ci_smoke.py
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: needs network or secrets (not run in default CI)
